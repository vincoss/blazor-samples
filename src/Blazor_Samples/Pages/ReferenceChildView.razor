@using Microsoft.Extensions.Logging
@inject ILogger<ReferenceChildView> logger

@code {
    public void ChildMethod(int value)
    {
        logger.LogInformation("Received {Value} in ChildMethod", value);
    }
}